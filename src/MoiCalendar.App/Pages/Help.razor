@page "/help"

<PageTitle>帮助与公测说明 | MoiCalendar</PageTitle>
<main class="settings-shell">
    <h1>帮助与公测说明</h1>
    <p>MoiCalendar 当前为受控公测版。重要日程请另存备份；新设备、离线使用和恢复操作建议先用测试日程确认。</p>
    <section class="settings-card">
        <h2>开始使用</h2>
        <ol>
            <li>打开日历，点击日期新建日程。无需云账户也能查看和修改本地日历。</li>
            <li>需要跨设备时，在设置中注册 MoiCalendar 云账户。若要求邮箱验证，打开验证邮件，再返回设置登录；也请检查垃圾邮件。</li>
            <li>仅登录不会导入全部历史本地日程。打开“同步状态”，执行“立即同步”，检查待处理数和冲突。同步前已有的日程请核对，必要时先导出备份。</li>
            <li>第二台设备使用同一账户登录并同步。请先确认两端都收到一条测试日程，再依赖云端数据。</li>
        </ol>
    </section>
    <section class="settings-card">
        <h2>同步与备份有什么区别？</h2>
        <dl>
            <dt>MoiCalendar 云</dt><dd>主账户云同步，用于已登录设备之间的增量变更。</dd>
            <dt>OneDrive</dt><dd>可选外部备份，使用独立 Microsoft 登录。现有处理会读回合并远端记录。</dd>
            <dt>WebDAV</dt><dd>可选外部备份，需自己的 HTTPS 服务器和浏览器跨域支持，也可能读回合并记录。</dd>
            <dt>本地 JSON</dt><dd>手动下载完整本地备份。文件由你保管，恢复前会预览并要求确认。</dd>
        </dl>
        <p>云账户导出与本地 JSON 备份范围不同：前者只包含云端数据；后者包含本机日历。下载前请确认你需要哪一种。</p>
    </section>
    <section class="settings-card">
        <h2>离线、冲突和恢复</h2>
        <p>离线前先完整打开一次应用。断网后可继续编辑本地日历，网络恢复后到同步状态重试。通知通道连接成功不代表日历已经同步完成。</p>
        <p>冲突会保留到处理完成。比较本地与云端版本后选择保留一方；无法确定时选择稍后处理并先导出备份。请勿反复清除浏览器存储。</p>
        <p>登录失效时重新登录。忘记密码可在设置请求恢复邮件；链接过期时重新请求。不要将验证或恢复链接发给他人。</p>
        <p>手机上可切换“日程”视图查看较长标题。平板和桌面支持月、周、日程视图。</p>
    </section>
    <section class="settings-card" id="account-deletion">
        <h2>设备、退出与删除账户</h2>
        <p>设置中的“我的设备”支持查看、重命名和移除设备。移除只阻止该设备继续应用云同步，不会终止其底层登录会话。</p>
        <p>退出云账户保留本地日历，Microsoft 登录单独管理。共享设备请使用独立浏览器资料。</p>
        <p>删除前先导出所需云端和本地数据。在设置 → 账户数据 → 删除云账户中阅读警告并确认；系统可能要求重新登录。默认保留本地日历，只有明确勾选后才删除本地数据。外部备份和已下载文件需自行管理。</p>
    </section>
    <section class="settings-card" id="support">
        <h2>支持与反馈</h2>
        <p>公测支持联系方式尚待维护者确认。受邀测试者请通过收到邀请的渠道反馈；公开发布前必须补充可用的联系渠道。</p>
        <p>反馈时提供页脚版本、设备类型、发生时间及不含隐私的复现步骤。同步状态页可导出运行诊断；发送前请检查内容。不要提供密码、登录链接、令牌或完整日历备份。</p>
        <a href="privacy">隐私说明草稿</a> · <a href="terms">使用条款草稿</a>
    </section>
</main>
