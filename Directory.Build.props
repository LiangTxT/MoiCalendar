<Project>
  <PropertyGroup>
    <!-- Controlled beta candidate; override Version explicitly for the next release. -->
    <Version Condition="'$(Version)' == ''">2.0.30-beta.1</Version>
    <AssemblyVersion>2.0.30.0</AssemblyVersion>
    <FileVersion>2.0.30.0</FileVersion>
  </PropertyGroup>
</Project>
